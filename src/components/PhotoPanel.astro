---
import type { ImageValue } from "emdash";
import ActionRail from "./ActionRail.astro";
import FeedCaption from "./FeedCaption.astro";
import PhotoSlides from "./PhotoSlides.astro";
import { titleEchoesCaption } from "../utils/text";

/**
 * One full-viewport photo panel in the swipe feed, laid out exactly like
 * VideoPanel so the two read as one feed. The box takes the cover's own
 * aspect ratio — a portrait shot isn't chopped to a landscape frame — and a
 * carousel swipes sideways inside it.
 */
interface Props {
	title: string;
	slides: ImageValue[];
	caption?: string | null;
	location?: string | null;
	date?: string | null;
	shareUrl: string;
	index: number;
	contentId: string;
}

const { title, slides, caption, location, date, shareUrl, index, contentId } = Astro.props;

const cover = slides[0];
const ratio = cover?.width && cover?.height ? cover.width / cover.height : 1;
// The title is usually just the caption's first line, clipped; show the
// caption's full text instead of both.
const showTitle = !titleEchoesCaption(title, caption);
const kicker = [location, date].filter(Boolean).join(" · ");
---

<section class="panel" data-panel aria-label={title}>
	<div class="media">
		<div class="box" style={`--arn:${ratio.toFixed(4)}`}>
			<PhotoSlides slides={slides} alt={title} eager={index < 2} />
		</div>
		<ActionRail collection="photos" contentId={contentId} shareUrl={shareUrl} shareTitle={title} />
		{
			index === 0 && (
				<p class="swipe-hint" data-swipe-hint>
					Swipe up for more ↑
				</p>
			)
		}
	</div>

	<div class="text">
		<FeedCaption kicker={kicker || null} title={title} caption={caption} showTitle={showTitle} />
	</div>
</section>

<style>
	.panel {
		height: 100%;
		scroll-snap-align: start;
		scroll-snap-stop: always;
		display: grid;
		grid-template-rows: minmax(0, 1fr) auto;
		padding-top: env(safe-area-inset-top, 0px);
		box-sizing: border-box;
	}

	.media {
		position: relative;
		min-height: 0;
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 8px 0;
		container-type: size;
	}

	.box {
		position: relative;
		width: min(100%, calc(100cqh * var(--arn)));
		aspect-ratio: var(--arn);
		background: #000;
		border-radius: 12px;
		overflow: hidden;
	}

	.swipe-hint {
		position: absolute;
		top: 16px;
		left: 50%;
		z-index: 3;
		transform: translateX(-50%);
		margin: 0;
		padding: 7px 14px;
		border-radius: 999px;
		background: rgb(255 255 255 / 0.16);
		color: #fff;
		font-size: 0.8125rem;
		white-space: nowrap;
		backdrop-filter: blur(10px);
		-webkit-backdrop-filter: blur(10px);
		transition: opacity 300ms;
	}

	.swipe-hint.is-gone {
		opacity: 0;
		pointer-events: none;
	}

	.text {
		padding: 12px var(--spacing-5) calc(env(safe-area-inset-bottom, 0px) + 96px);
	}

	@media (prefers-reduced-motion: reduce) {
		.swipe-hint {
			transition: none;
		}
	}
</style>
